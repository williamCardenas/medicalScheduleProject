{% extends 'base.html.twig' %}

{% block title %}{{ 'titulo.medico.index'|trans }}
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1 class="page-head-line">{{ 'titulo.medico.index'|trans }}</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <a href="{{ path('medico_new') }}" class="btn btn-primary">{{ 'botao.novo'|trans }}</a>
                    </div>
                    <div class="panel-body">
                        <div class="table-responsive">
                            <table class="table table-striped table-bordered table-hover">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Nome</th>
                                        <th>NumeroDocumento</th>
                                        <th>actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for medico in medicos %}
                                        <tr>
                                            <td>{{ medico.id }}</td>
                                            <td>{{ medico.nome }}</td>
                                            <td>{{ medico.numeroDocumento }}</td>
                                            <td>
                                                <a class="btn btn-default" href="{{ path('medico_show', {'id': medico.id}) }}">
                                                    {{'botao.exibir'|trans}}
                                                </a>
                                                <a class="btn btn-default" href="{{ path('medico_edit', {'id': medico.id}) }}">
                                                    {{'botao.editar'|trans}}
                                                </a>
                                                <a class="btn btn-default" href="{{ path('agenda_index', {'medicoId': medico.id}) }}">
                                                    {{'botao.agenda'|trans}}
                                                </a>
                                            </td>
                                        </tr>
                                    {% else %}
                                        <tr>
                                            <td colspan="4">no records found</td>
                                        </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>
{% endblock %}