{% extends 'base.html.twig' %}

{% block title %}{{ 'titulo.medico.novo'|trans }}{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1 class="page-head-line">{{ 'titulo.medico.novo'|trans }}</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            {{ include( 'medico/_form.html.twig') }} 
            <a class="btn btn-default" href="{{ path('medico_index') }}">
                {{'botao.voltar'|trans}}
            </a>
        </div>
    </div>
</div>
{% endblock %}