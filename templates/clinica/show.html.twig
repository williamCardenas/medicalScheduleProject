{% extends 'base.html.twig' %}

{% block title %}Clinic{% endblock %}

{% block body %}
    <h1>Clinic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ clinic.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ clinic.name }}</td>
            </tr>
            <tr>
                <th>ClientId</th>
                <td>{{ clinic.clientId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('clinic_index') }}">back to list</a>

    <a href="{{ path('clinic_edit', {'id': clinic.id}) }}">edit</a>

    {{ include('clinic/_delete_form.html.twig') }}
{% endblock %}