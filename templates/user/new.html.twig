{% extends 'base.html.twig' %} 

{% block title %}New Usuario{% endblock %} 

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1 class="page-head-line">{{ 'titulo.usuario.novo'|trans }}</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            {{ include( 'user/_form.html.twig') }} 
            <a class="btn btn-default" href="{{ path('user_index') }}">
                {{'botao.voltar'|trans}}
            </a>
        </div>
    </div>
</div>
{% endblock %}